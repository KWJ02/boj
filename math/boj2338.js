// 문제 : 긴자리 계산

const solution = (a, b) => {
	console.log(String(BigInt(a) + BigInt(b)));
	console.log(String(BigInt(a) - BigInt(b)));
	console.log(String(BigInt(a) * BigInt(b)));
};

solution(1, -1);
